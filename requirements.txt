# Runtime dependencies are mirrored in pyproject.toml [project.dependencies].
# This file additionally lists dev/CI tools. Keep the three sections in sync.

# Core
pandas>=2.2.0,<3.0.0
numpy>=1.26.0
pyarrow>=14.0.0
pyyaml>=6.0
python-dateutil>=2.8.0

# Validation
pandera>=0.20.0

# Dashboard
streamlit>=1.28.0
plotly>=5.18.0

# Web entrypoint (Vercel) — see main.py
fastapi>=0.110.0

# SQL
# sqlite3 is stdlib

# Dev / CI
ruff>=0.1.0
mypy>=1.6.0
pytest>=7.4.0
pytest-cov>=4.1.0
httpx>=0.27.0
uvicorn>=0.27.0
types-PyYAML>=6.0.0
types-python-dateutil>=2.8.0

# Optional orchestration
# prefect>=2.14.0
